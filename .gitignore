.claude/
.idea/
target/
.DS_Store
.tmp
*.iml
output.txt
config/helm/*
!config/helm/icegate/
!config/helm/auth-proxy/
config/kustomize/**/charts
# Local-only cargo overrides (e.g. `[patch]` for icegatetech/jobmanager); `.cargo/audit.toml`
# is shared configuration and stays under version control.
/.cargo/config.toml
__pycache__/
# Built by `make install-deps` for the Python chart checks in scripts/.
.venv/
