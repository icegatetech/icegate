# AWS credentials for Nessie S3 access (dev-only).
# Production: use IRSA (EKS) or Workload Identity (GKE) instead.
apiVersion: v1
kind: Secret
metadata:
  name: icegate-aws-credentials
  namespace: infra
type: Opaque
stringData:
  aws-access-key-id: key-id
  aws-secret-access-key: FLkey
