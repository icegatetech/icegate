# AWS credentials for IceGate components (dev-only).
# Production: use IRSA (EKS) or Workload Identity (GKE) instead.
apiVersion: v1
kind: Secret
metadata:
  name: icegate-aws-credentials
  namespace: icegate
type: Opaque
stringData:
  aws-access-key-id: rustfsadmin
  aws-secret-access-key: rustfsadmin
