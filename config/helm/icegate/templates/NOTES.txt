IceGate has been deployed to namespace {{ .Release.Namespace }}.

{{- if .Values.query.enabled }}

=== Query Service ===

{{- if .Values.query.loki.enabled }}
Loki API (port {{ .Values.query.loki.port }}):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "icegate.componentName" (dict "context" . "component" "query") }} {{ .Values.query.loki.port }}:{{ .Values.query.loki.port }}
{{- end }}

{{- if .Values.query.prometheus.enabled }}
Prometheus API (port {{ .Values.query.prometheus.port }}):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "icegate.componentName" (dict "context" . "component" "query") }} {{ .Values.query.prometheus.port }}:{{ .Values.query.prometheus.port }}
{{- end }}

{{- if .Values.query.tempo.enabled }}
Tempo API (port {{ .Values.query.tempo.port }}):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "icegate.componentName" (dict "context" . "component" "query") }} {{ .Values.query.tempo.port }}:{{ .Values.query.tempo.port }}
{{- end }}

{{- end }}

{{- if .Values.ingest.enabled }}

=== Ingest Service ===

{{- /* The port forwarded here is the one the Service publishes, not the one the
       receiver binds: `port-forward` against a `svc/` names a port of that
       Service, and the two are different numbers as soon as a sidecar owns the
       published ports. `icegate.ingestPublishedPort` is what service-ingest.yaml
       publishes, so this command addresses what that Service actually carries. */}}
{{- if .Values.ingest.otlpGrpc.enabled }}
{{- $grpcPort := include "icegate.ingestPublishedPort" (dict "context" . "signal" "otlpGrpc") }}
OTLP gRPC (port {{ $grpcPort }}):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "icegate.componentName" (dict "context" . "component" "ingest") }} {{ $grpcPort }}:{{ $grpcPort }}
{{- end }}

{{- if .Values.ingest.otlpHttp.enabled }}
{{- $httpPort := include "icegate.ingestPublishedPort" (dict "context" . "signal" "otlpHttp") }}
OTLP HTTP (port {{ $httpPort }}):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "icegate.componentName" (dict "context" . "component" "ingest") }} {{ $httpPort }}:{{ $httpPort }}
{{- end }}

{{- end }}

{{- if .Values.maintain.enabled }}

=== Maintain Service ===

Long-running Parquet compaction (rewrites small data files into larger ones,
scanning every {{ .Values.maintain.compaction.jobsmanager.scanIntervalSecs }}s). It has no API; follow its progress with:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=maintain -f
{{- end }}
