{{- if and .Values.ingest.enabled .Values.ingest.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "icegate.componentName" (dict "context" . "component" "ingest") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "icegate.componentLabels" (dict "context" . "component" "ingest") | nindent 4 }}
  {{- with .Values.ingest.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingest.ingress.className }}
  ingressClassName: {{ .Values.ingest.ingress.className }}
  {{- end }}
  {{- if .Values.ingest.ingress.tls }}
  tls:
    {{- toYaml .Values.ingest.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingest.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "icegate.componentName" (dict "context" $ "component" "ingest") }}
                port:
                  number: {{ .port }}
          {{- end }}
    {{- end }}
{{- end }}
